{
  "name": "@vmd/analytics",
  "version": "0.0.0",
  "private": true,
  "description": "GA4 (server-side Measurement Protocol) + Plausible wrappers. Consent-aware. One track() surface.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo source-only",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@vmd/config": "workspace:*"
  }
}
