# assets — logos · icons · illustrations

Supplied brand SVGs (all four colourways + favicon), one line-icon set (1.5px,
24px grid, inline SVG), pathway illustrations. Assets are NOT tokens (ARCHITECTURE.md §2.1).
Placeholders committed in Module 1 once production SVGs are exported from the Mark.
