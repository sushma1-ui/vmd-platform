# @vmd/seo

Typed-data-in, schema/meta-out. Organisation/LocalBusiness/Person/Article/FAQ/
Breadcrumb JSON-LD, meta + OG/Twitter, canonical, sitemap. NAP flows from
`@vmd/config` so it is identical everywhere (Blueprint §13.5).
