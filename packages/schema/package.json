{
  "name": "@vmd/schema",
  "version": "0.0.0",
  "private": true,
  "description": "The shared contract. Zod schemas imported by BOTH apps/web (validation) and apps/cms (collections). One definition of a Lead, a Consultation, a HealthCheck submission. Validation IS schema (ARCHITECTURE.md §2.4).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo source-only",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vmd/config": "workspace:*",
    "@types/node": "^26.1.1"
  }
}
