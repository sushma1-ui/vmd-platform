# @vmd/email
One public API — `sendTransactional()`. Postmark under the hood; swappable without
touching callers. Confirmation emails carry the 6-document prep checklist
(commitment device, Blueprint §9.4).
