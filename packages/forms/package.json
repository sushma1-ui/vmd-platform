{
  "name": "@vmd/forms",
  "version": "0.0.0",
  "private": true,
  "description": "Validation, submission, success/error handling, spam + rate limiting orchestration. Consumes @vmd/schema. Field logic, not field components (those are ui/apps).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo source-only",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@vmd/schema": "workspace:*"
  }
}
