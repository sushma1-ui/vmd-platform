# supabase
- `migrations/` — numbered, forward-only, committed to Git.
- `policies/`   — RLS, ONE file per table. RLS protects browser-direct access
  (anon/authenticated JWTs). Region: Sydney (AU data residency, Blueprint §17).
