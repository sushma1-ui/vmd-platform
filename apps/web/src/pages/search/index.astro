---
import SimplePage from '../../components/content/SimplePage.astro';
import { getCtaLinks } from '../../lib/cms.ts';
const { healthCheckUrl } = await getCtaLinks();
---

<SimplePage
  title="Search"
  description="Search Visa & Migration Doctors."
  eyebrow="Search"
  lead="Find a page, a subclass, or an answer."
  emptyState="Site search is wired in the search module."
  cta={{ href: healthCheckUrl, label: 'Or take the Health Check' }}
/>
